# Technology Stack

**Analysis Date:** 2026-02-16

## Languages

**Primary:**
- TypeScript 5.9.2 - All source code (`app/`, `server/`, `shared/`, `lib/`, `components/`)
- JavaScript - Configuration files (`metro.config.js`, `babel.config.js`, `eslint.config.js`)
- Bash/Shell - Build scripts (`scripts/`)

**Secondary:**
- CSS/Styling - Via React Native and web components
- JSON - Configuration and manifest files

## Runtime

**Environment:**
- Node.js (version not specified in lockfiles, inferred from npm scripts)

**Package Manager:**
- npm (8.x or later - uses `package-lock.json`)
- Lockfile: `package-lock.json` present

## Frameworks

**Core:**
- Expo 54.0.27 - React Native framework with managed services
- React 19.1.0 - UI framework (web and native)
- React Native 0.81.5 - Cross-platform mobile runtime
- Expo Router 6.0.17 - File-based routing for Expo (similar to Next.js)

**State Management & Data:**
- TanStack React Query 5.83.0 - Server state management with `QueryClientProvider`
- AsyncStorage 2.2.0 - Persistent local storage for React Native
- Drizzle ORM 0.39.3 - Type-safe SQL ORM for PostgreSQL
- Zod 3.24.2 - Runtime schema validation
- drizzle-zod 0.7.0 - Integration between Drizzle and Zod schemas

**Backend:**
- Express 5.0.1 - HTTP server framework with middleware support
- Node HTTP - Native Node.js HTTP server wrapper

**Navigation & UI:**
- React Native Gesture Handler 2.28.0 - Touch gesture handling
- React Native Reanimated 4.1.1 - Performant animations
- React Native Screens 4.16.0 - Native screen navigation
- React Native Safe Area Context 5.6.0 - Safe area insets handling
- React Native Keyboard Controller 1.20.6 - Keyboard interaction management
- Expo Status Bar 3.0.9 - Status bar configuration
- Expo Linking 8.0.10 - Deep linking support
- Expo Web Browser 15.0.10 - Web browser control

**Media & Assets:**
- Expo Image 3.0.11 - Optimized image handling
- Expo Image Picker 17.0.9 - Photo/video selection
- Expo File System 19.0.21 - File system access
- Expo Sharing 14.0.8 - Share functionality
- React Native SVG 15.12.1 - SVG rendering
- Expo Symbols 1.0.8 - Native system symbols
- Expo Vector Icons 15.0.3 - Icon library
- Expo Google Fonts (Inter) 0.4.2 - Typography

**Graphics & Effects:**
- Expo Linear Gradient 15.0.8 - Gradient backgrounds
- Expo Glass Effect 0.1.4 - iOS glass morphism effects
- Expo Blur 15.0.8 - Blur effects

**System Access:**
- Expo Location 19.0.8 - Location services
- Expo Haptics 15.0.8 - Haptic feedback
- Expo Constants 18.0.11 - App configuration constants

**Forms & Validation:**
- zod-validation-error 3.4.0 - Formatted Zod error messages

**Utilities:**
- WebSocket (ws) 8.18.0 - WebSocket support (installed but not actively used in explored code)
- http-proxy-middleware 3.0.5 - Proxy middleware for Express
- patch-package 8.0.0 - Post-install package patching
- tsx 4.20.6 - TypeScript execution for Node.js scripts
- Expo Fetch - Native fetch API from Expo (used in `lib/query-client.ts`)

**Web Support:**
- React DOM 19.1.0 - React rendering for web
- React Native Web 0.21.0 - RN component bridge to web

## Build & Development Tools

**Runtime Transformation:**
- Babel 7.25.2 - JavaScript transpiler with Expo preset
- babel-plugin-react-compiler 19.0.0-beta - React compiler plugin for optimization
- Metro - JavaScript bundler for Expo (configured in `metro.config.js`)

**TypeScript Compiler:**
- TypeScript 5.9.2 - Type checking and compilation
- `extends: "expo/tsconfig.base"` - Expo-managed TypeScript config

**Build Scripting:**
- esbuild - Used in `npm run server:build` for Node.js bundling
- Node scripts system - Custom build scripts in `scripts/`

**Linting & Code Quality:**
- ESLint 9.31.0 - JavaScript/TypeScript linting
- eslint-config-expo 10.0.0 - Expo-specific linting rules

## Configuration Files

**TypeScript:**
- `tsconfig.json` - Compiler options with path aliases (`@/*`, `@shared/*`)

**Runtime:**
- `app.json` - Expo configuration (name, slug, plugins, experiments)
- `metro.config.js` - Metro bundler configuration
- `babel.config.js` - Babel configuration with React compiler
- `eslint.config.js` - ESLint configuration
- `drizzle.config.ts` - Drizzle ORM configuration for PostgreSQL migrations

**Scripts:**
- `package.json` scripts include:
  - `expo:dev` - Development with Replit environment variables
  - `server:dev` - Node server development with tsx
  - `server:build` - esbuild bundling for production
  - `server:prod` - Production server execution
  - `db:push` - Push database migrations via Drizzle
  - `lint` / `lint:fix` - ESLint commands

## Database

**ORM:**
- Drizzle ORM 0.39.3 with PostgreSQL dialect
- Schema: `shared/schema.ts` (defines `users` table with Zod integration)
- Migrations: Generated by Drizzle Kit (output directory: `./migrations`)

**Database:**
- PostgreSQL (referenced in `drizzle.config.ts` with `dialect: "postgresql"`)
- Client: `pg` 8.16.3 - Native PostgreSQL driver for Node.js

## Environment Configuration

**Required Environment Variables:**
- `DATABASE_URL` - PostgreSQL connection string (required for Drizzle migrations)
- `EXPO_PUBLIC_DOMAIN` - Public domain for API requests in client
- `REPLIT_DEV_DOMAIN` - Replit development domain
- `REPLIT_DOMAINS` - Comma-separated list of Replit production domains
- `PORT` - Server port (defaults to 5000)
- `NODE_ENV` - Environment (development/production)

**Expo Configuration:**
- `EXPO_PACKAGER_PROXY_URL` - Proxy URL for packager
- `REACT_NATIVE_PACKAGER_HOSTNAME` - Packager hostname

## Platform Support

**Mobile:**
- iOS 12+ (configured in `app.json` with bundle identifier `com.myapp`)
- Android (configured in `app.json` with package `com.myapp`)

**Web:**
- React DOM-based web support via Expo Web and React Native Web
- Static build system with manifest serving

**Development:**
- New Architecture enabled (`"newArchEnabled": true` in `app.json`)
- Typed routes enabled (`"typedRoutes": true`)
- React Compiler enabled (`"reactCompiler": true`)

## Experimental & Advanced Features

**React Compiler:**
- Integrated via babel-plugin-react-compiler (beta version)
- Enabled for automatic optimization of React components

**Expo New Architecture:**
- Enabled for improved performance and bridge efficiency

**Typed Routes:**
- Expo Router with full type safety for navigation

---

*Stack analysis: 2026-02-16*
